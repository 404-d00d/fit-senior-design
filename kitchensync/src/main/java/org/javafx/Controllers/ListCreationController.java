package org.javafx.Controllers;

import org.javafx.Main.Main;

import java.util.ArrayList;
import javafx.fxml.FXML;

public class ListCreationController {


    // Method to create list for ingredients/cooking equipment
    @FXML
    private void addItemToShoppingList(){
        // search bar that adds items to list
        // run check if item exists
        //      if item exists add it to database
        //      then add item into ui and add a button to the side that lets you remove it
        //      if not do not enter it
    }

    @FXML
    private void removeItemFromShoppingList(){
        // when button on side is clicked, should remove item from list and UI.
    }

}
